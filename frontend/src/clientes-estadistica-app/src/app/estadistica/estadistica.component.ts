import { Component, OnInit } from '@angular/core';

/**
 * Componente para mostrar estadísticas.
 * Este componente es responsable de mostrar las estadísticas, aunque actualmente no tiene lógica implementada.
 */
@Component({
  selector: 'app-estadistica',
  templateUrl: './estadistica.component.html',
  styleUrls: ['./estadistica.component.css'],
})
export class EstadisticaComponent implements OnInit {

  /**
   * Crea una instancia del componente `EstadisticaComponent`.
   */
  constructor() {}

  /**
   * Inicializa el componente.
   * Se puede utilizar para realizar la configuración inicial del componente.
   */
  ngOnInit(): void {}
}
